export type Author = {
  email: string;
  first: string;
  last: string;
};

export type Book = {
  title: string;
  isbn: string;
  authors: string;
  description: string;
};

export type Magazines = {
  title: string;
  isbn: string;
  authors: string;
  publishedAt: string;
};
